/* Index route */

var router = require('express').Router(),
    session = require('../controllers/session'),
    ctrl = require('../controllers/index')

router.get('/', ctrl.index)

router.get('/favicon.ico', ctrl.favicon)

module.exports = router
